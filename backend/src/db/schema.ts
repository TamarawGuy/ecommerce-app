// Drizzle schema — tables are introduced per vertical slice.
// #4 adds `categories`, #5 adds `products`/`variants`, etc.
// Kept intentionally empty for the walking skeleton (#3) so drizzle-kit is wired
// and ready; the first real migration lands with the category tree.
export {};
