// The Wishlist tab's product-detail route. The screen is shared with the Home
// and Categories tabs' detail routes (see src/components/ProductDetailScreen);
// this file just mounts it within the Wishlist stack so the tab bar stays
// visible when a shopper opens a saved item.
export { ProductDetailScreen as default } from "@/src/components/ProductDetailScreen";
