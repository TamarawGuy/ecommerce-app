// The Home tab's product-detail route. Shares the screen with the Categories
// tab's detail route (see src/components/ProductDetailScreen); this file just
// mounts it within the Home stack so the tab bar stays visible.
export { ProductDetailScreen as default } from "@/src/components/ProductDetailScreen";
