import { CategoryBrowser } from "@/src/components/CategoryBrowser";

// Top of the Categories tab: the root departments (Men, Women, Accessories).
export default function CategoriesScreen() {
  return <CategoryBrowser />;
}
